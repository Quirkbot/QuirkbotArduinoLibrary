// Math
-Sine  // @Sin
-Cosine  // @Cos
-Tangent  // @Tan
-ArcTangent  // @Atan

Minimum // @Min
Maximum // @Min
Constrain //
Mapping // @Map
Normalize
-Power // @Pow
-Logarithm // @Log
-SquareRoot // @Sqrt
-Absolut // @Abs
-Round
-Floor
-Ceiling // @Ceil

-Addition // @Add
-Subtraction // @Sub
Multiplication // @Mult
Division // @Div
Modulo // @Mod
*Operation

// Logic (base LogicNode)
Equal
	Inputs
		in [float]
		reference [float]
	Outputs
		out [float] // 0.0 or 1.0
	Events
		onTrue
		onFalse
NotEqual // @Different
	Inputs
		in [float]
		reference [float]
	Outputs
		out [float] // 0.0 or 1.0
	Events
		onTrue
		onFalse
LessThan // @Less
	Inputs
		in [float]
		reference [float]
	Outputs
		out [float] // 0.0 or 1.0
	Events
		onTrue
		onFalse
GreaterThan // @More
	Inputs
		in [float]
		reference [float]
	Outputs
		out [float] // 0.0 or 1.0
	Events
		onTrue
		onFalse
*If

// Logic
Swicth (Maybe only need one switch with 10 ins)
Switch2 // @Swicth
Switch3
Switch4
Switch5
Switch6

// Signal Processing
Average
Peaks
Valleys

// Generators
Time
Random // @Rand
Wave // @Osc
Ramp // @Fade
Pulse

Timeout // @Delay
Interval // @Tick or @Metronome
	Inputs
		interval [float]

		start()
		stop()
	Outputs
		out
	Events
		onTick
List
	Inputs
		items [vector]
		index [int]
	Outputs
		out
Sequence
	Inputs
		items [vector]
		interval [float]
		loop [bool]

		start()
		stop()
	Outputs
		out
	Events
		onTick
		onStart
		onEnd

// Util
Logger // @Print
	Inputs
		in

		
		in0,
		in1,
		in2,
		in3,
		in4,
		in5,
		in6,
		in7,
		in8,
		in9,

		first
		second
		third

		one two three four five
		a,
		b,
		c,
		d

		aLabel

// Communication